name: TEST_VOLKAN

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  check-dependencies:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Check for dependency updates
        run: |
          mvn versions:display-property-updates versions:display-parent-updates -DgenerateBackupPoms=false \
            | grep '\->' \
            | awk -F ' ' '$2 != $4 { print $0 }' > dependency-updates.txt
        
        
        
      - name: Check if dependency-updates.txt is empty
        id: check_updates
        run: echo "::set-output name=isEmpty::$(test -s dependency-updates.txt || echo true)"

      - name: Perform updates if not empty
        if: steps.check_updates.outputs.isEmpty != 'true'
        run: |
          # Your update commands here
          echo "Performing dependency updates"

      - name: Notify if empty
        if: steps.check_updates.outputs.isEmpty == 'true'
        run: echo "No dependency updates found"
        
        
      - name: Check if dependency-updates.txt is empty
        id: check_empty_file
        run: |
          if [ ! -s dependency-updates.txt ]; then
            echo "Dependency updates file is empty, skipping further steps."
            touch skip-run
          fi

      - name: Comment on pull request (if updates found)
        uses: actions/github-script@v4
        if: steps.check_empty_file.outputs.skip-run != 'true'
        with:
          github-token: ${{ secrets.PR_CHECK }}
          script: |
            const fs = require('fs');
            const updates = fs.readFileSync('dependency-updates.txt', 'utf8');
            const commentBody = `❌ WARNING: The following dependencies or microservices have updates available:\n\n${updates}`;
            const { owner, repo, number } = context.issue;
            github.pulls.createReview({
              owner: owner,
              repo: repo,
              pull_number: number,
              body: commentBody
            });
